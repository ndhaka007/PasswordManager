package constants

const (
	// Request keys
	Request = "request"

	// StatusCode key where status code has to be set
	StatusCode = "status_code"

	// Response Keys where response has to be written
	Response = "response"

	// Headers key for headers
	Headers = "headers"

	// RequestID holds the unique request identifier for the request
	RequestID = "request_id"

	// ExternalRequestID holds the unique request request identifier
	ExternalRequestID = "external_request_id"

	// TaskID is the key to hold the task id of the process
	TaskID = "task_id"
)